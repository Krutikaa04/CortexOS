# CortexOS Studio configuration

# URL of the CortexOS runtime API (local Docker runtime)
NEXT_PUBLIC_CORTEX_API_URL=http://localhost:8000

# Set to 1 for the public Vercel deployment: Studio runs as a replay demo
# against recorded real traces in public/traces/, without a live runtime.
NEXT_PUBLIC_DEMO_MODE=0
